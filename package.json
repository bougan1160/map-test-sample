{
  "name": "map-test-sample",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "http-server -p 8080",
    "test": "npm run test:puppeteer && npm run test:playwright",
    "test:puppeteer": "start-server-and-test start http://localhost:8080 'jest --config=test/puppeteer/jest.config.js'",
    "test:playwright": "start-server-and-test start http://localhost:8080 'playwright test --config=test/playwright/playwright.config.js'"
  },
  "author": "Yasuaki KAWAKAMI",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "http-server": "^14.1.1",
    "jest": "^29.5.0",
    "jest-image-snapshot": "^6.4.0",
    "puppeteer": "^24.10.0",
    "start-server-and-test": "^2.0.12"
  }
}
